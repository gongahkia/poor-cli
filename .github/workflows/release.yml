name: Seuss Release on push

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: x86_64-unknown-linux-gnu
          - os: macos-latest
            artifact: apple-darwin
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: haskell-actions/setup@v2
        with:
          ghc-version: "9.6.6"
          cabal-version: "3.10.3.0"

      - name: Update package index
        run: cabal update

      - name: Build
        run: cabal build exe:seuss

      - name: Install executable
        run: |
          mkdir -p dist/bin
          cabal install exe:seuss --installdir=dist/bin --overwrite-policy=always

      - name: Package
        run: |
          tar czf seuss-${{ github.ref_name }}-${{ matrix.artifact }}.tar.gz -C dist/bin seuss

      - uses: actions/upload-artifact@v4
        with:
          name: seuss-${{ matrix.artifact }}
          path: seuss-*.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: List artifacts
        run: ls -lh seuss-*.tar.gz

      - uses: softprops/action-gh-release@v2
        with:
          files: seuss-*.tar.gz
          generate_release_notes: true
