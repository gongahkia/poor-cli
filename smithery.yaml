name: swee-sg
description: Swee SG MCP gateway for policy-governed Singapore public-data tools, Shield audit trails, and Pulse city signals.
transport: stdio
install: npm install && npm run build && node packages/mcp-server/dist/index.js
