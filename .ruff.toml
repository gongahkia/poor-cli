# Ruff configuration
line-length = 100
target-version = "py39"

[lint]
select = [
    "E9",   # syntax/runtime parse errors
    "F63",  # invalid syntax in %-format strings
    "F7",   # undefined local names
    "F82",  # undefined names in __all__ / locals
]
ignore = []
