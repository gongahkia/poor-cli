/// Shared multiplayer helpers for the TUI library crate.
///
/// The binary's multiplayer runtime glue currently lives in `src/multiplayer.rs`.
pub fn multiplayer_module_ready() -> bool {
    true
}
