run:
  timeout: 5m
  go: '1.24'

linters:
  disable-all: true
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt

issues:
  exclude-dirs:
    - bin
    - dist
    - .venv
    - poor_cli
    - nvim-poor-cli
