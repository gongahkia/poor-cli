# homebrew-tap

Formula source for `gongahkia/homebrew-tap`.

GoReleaser publishes `Formula/gocli-poor.rb` here on tag release using `HOMEBREW_TAP_GITHUB_TOKEN`.

Install after release:

```sh
brew install gongahkia/tap/gocli-poor
```
