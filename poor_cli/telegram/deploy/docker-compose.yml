version: '3.8'
services:
  poor-cli-bot:
    build:
      context: ../../../
      dockerfile: Dockerfile
    command: python -m poor_cli telegram --token ${TELEGRAM_TOKEN}
    env_file: .env
    volumes:
      - bot-data:/home/pooruser/.poor-cli
    restart: unless-stopped
volumes:
  bot-data:
