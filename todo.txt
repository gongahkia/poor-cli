# poor-cli Evolution Map — Generated 2026-03-05
# Priority (B) = audit-suggested improvement
# Tags: +refactor +feature +infra +security +bugfix +cleanup +neovim +multiplayer +provider +plan +checkpoint

# ============================================================================
# PHILOSOPHICAL ALIGNMENT — Fulfill the project's original promise
# ============================================================================



# ============================================================================
# DECOMPOSE main.rs (8.4K LOC monolith → modules)
# ============================================================================

(B) Extract the main event loop (crossterm event polling, RPC message handling, tick processing) from main.rs into poor-cli-tui/src/event.rs +refactor
(B) Extract all slash-command parsing and dispatch logic from main.rs into poor-cli-tui/src/commands.rs +refactor
(B) Extract RPC response handling (streaming chunks, tool events, permission prompts, plan review) from main.rs into poor-cli-tui/src/handler.rs +refactor
(B) Extract multiplayer host/join logic from main.rs into poor-cli-tui/src/multiplayer.rs +refactor

# ============================================================================
# CHECKPOINT SYSTEM — Consolidate and harden
# ============================================================================

(B) Reduce checkpoint_strategies.py from 6 classes to 3 by merging ProjectDetector+FileFilter into one class and SmartCompressor+PartialCheckpointStrategy into one class +checkpoint +refactor

# ============================================================================
# PLAN SYSTEM — Make it reliable
# ============================================================================


# ============================================================================
# MULTIPLAYER — Refine as core differentiator
# ============================================================================

(B) Audit poor_cli/multiplayer.py for race conditions in RoomState between request_queue operations and dispatch_lock; add integration test covering concurrent prompter requests to the same room +multiplayer +bugfix
(B) Add connection health checks (ping/pong heartbeat) to multiplayer.py WebSocket connections so stale connections are detected and cleaned up +multiplayer +feature
(B) Add a /kick command to multiplayer host that revokes a specific connection by ID and closes its WebSocket +multiplayer +feature
(B) Add rate limiting per connection in multiplayer.py to prevent a single prompter from flooding the request queue +multiplayer +feature
(B) Add a room participant list RPC method so TUI clients can display who is connected, their roles, and connection duration +multiplayer +feature
(B) Add automatic reconnection logic in the TUI Rust client (poor-cli-tui) for multiplayer sessions when the WebSocket connection drops +multiplayer +feature
(B) Add an event broadcast mechanism so viewers see real-time streaming output from the active prompter's session, not just final results +multiplayer +feature

# ============================================================================
# NEOVIM PLUGIN — First-class citizen fixes and features
# ============================================================================

(B) Add a virtual text diagnostic integration that shows poor-cli suggestions inline as Neovim diagnostics (vim.diagnostic) rather than only in the chat panel +neovim +feature
(B) Add a telescope.nvim picker for checkpoint browsing and restoration directly from Neovim +neovim +feature
(B) Deduplicate the test file naming pattern logic between commands.lua and chat.lua into a shared utility function in a new nvim-poor-cli/lua/poor-cli/util.lua +neovim +refactor

# ============================================================================
# STABILITY — Error handling, security, hardening
# ============================================================================

(B) In poor_cli/api_key_manager.py, derive the Fernet encryption key using PBKDF2 from a passphrase instead of storing a raw key in a plaintext .keyfile +security +refactor
(B) In poor_cli/exceptions.py validate_file_path(), handle TOCTOU race by catching FileNotFoundError/PermissionError at actual usage sites rather than only during validation +security +bugfix
(B) Audit all 47 unwrap() calls in the Rust TUI codebase; replace panicking unwraps in event handling and RPC parsing paths with proper error propagation using Result/match +security +refactor
(B) In poor_cli/tools_async.py, replace broad Exception catches with specific exception types (FileOperationError, CommandExecutionError, etc.) in at least the 10 most critical tool execution paths +bugfix +refactor
(B) In poor_cli/file_cache.py, add a TTL/expiry mechanism so stale cache entries are evicted after a configurable duration +feature
(B) In poor_cli/error_recovery.py, consolidate the 25 except clauses by extracting a common error-to-suggestion mapping function that all handlers delegate to +refactor

# ============================================================================
# INFRA & CI
# ============================================================================

(B) In .github/workflows/tests.yml, add a retry mechanism (e.g., retry-on-error action) to the JSON-RPC stdio smoke test to handle transient startup delays +infra
(B) Add a completions uninstall mechanism to completions/install.sh or a separate completions/uninstall.sh script +infra +feature
(B) Expand the bash completion script (completions/poor-cli.bash) to include provider names, model names, and /api-key subcommands +infra +feature

# ============================================================================
# PROVIDER SYSTEM
# ============================================================================

(B) In poor_cli/providers/tool_translator.py from_provider_format(), add input structure validation to reject malformed tool definitions with a clear error message instead of silently producing broken output +provider +bugfix
(B) In poor_cli/providers/tool_translator.py, add unit tests covering round-trip translation (canonical → provider → canonical) for all 4 provider formats +provider +infra
(B) Abstract the manual provider-specific error parsing in server.py (Gemini/OpenAI/Anthropic error format differences) into provider-specific error_from_response() methods on each provider class +provider +refactor

# ============================================================================
# GENERAL CLEANUP
# ============================================================================

(B) Remove the bare except: clause in checkpoint_display.py and replace all other bare except: usages across the codebase with specific exception types +cleanup +bugfix
(B) Run ruff check --fix across the entire Python codebase to auto-fix any remaining linting issues flagged by the existing ruff config +cleanup
(B) Verify that the pyproject.toml entry_points for poor-cli and poor-cli-server resolve correctly after the server.py decomposition +cleanup +infra
