# poor-cli Evolution Map — Generated 2026-03-05
# Priority (B) = audit-suggested improvement
# Tags: +refactor +feature +infra +security +bugfix +cleanup +neovim +multiplayer +provider +plan +checkpoint
#
# CONTEXT: poor-cli is an AI coding assistant with a Python backend (JSON-RPC server),
# a Rust TUI frontend (ratatui+crossterm), a Neovim plugin (Lua), and multiplayer
# WebSocket support. The codebase already has modules extracted into:
#   - poor-cli-tui/src/{app,input,markdown,rpc,theme,ui,helpers,watcher}.rs
#   - poor_cli/server/{__init__,types,error_formatter,transport}.py
#   - poor_cli/_server.py (main server class, still large)
#
# PRIOR WORK: 35+ tasks already completed in the previous session including:
#   watcher.rs + helpers.rs extraction, all checkpoint bugfixes, all plan system
#   bugfixes, neovim rpc/chat/commands fixes, Dockerfile non-root, PBKDF2 key
#   derivation, bare except elimination, CI retry, completions, bash completions,
#   tool_translator validation, file_cache TTL+deque, configurable timeouts, etc.

# ============================================================================
# DECOMPOSE main.rs (8.4K LOC monolith → modules)
# ============================================================================

(B) Extract the main event loop from main.rs into poor-cli-tui/src/event.rs +refactor
# PURPOSE: main.rs is ~8.4K LOC. The event loop (crossterm event polling, RPC
#   message handling via ServerMsg enum, tick processing) is the core loop in main().
# WHAT TO DO:
#   1. Read main.rs main() function — it contains a loop that polls crossterm::event,
#      receives from mpsc::Receiver<ServerMsg>, and processes ticks.
#   2. Create event.rs with a pub fn run_event_loop(...) or similar that encapsulates
#      the select! / match logic for keyboard events, ServerMsg variants, and ticks.
#   3. The ServerMsg enum (line ~65, 16 variants) should move to event.rs or a shared
#      types module since it's the message protocol between background threads and UI.
#   4. Update lib.rs to declare `pub mod event;`
#   5. Update main.rs main() to call into event.rs.
# DONE WHEN: `cargo build` succeeds, `cargo test` passes all 21 tests, and main.rs
#   main() is substantially shorter (event loop logic lives in event.rs).

(B) Extract all slash-command parsing and dispatch logic from main.rs into poor-cli-tui/src/commands.rs +refactor
# PURPOSE: handle_slash_command() spans lines ~1882–5694 (3,814 lines!) and is the
#   single largest function. It's a giant match/if-else chain for /help, /quit, /diff,
#   /watch, /qa, /provider, /config, /plan, /multiplayer, etc.
# WHAT TO DO:
#   1. Read handle_slash_command() in main.rs — note it takes &mut App, tx, rpc_cmd_tx,
#      watch_state, qa_watch_state, and other state as parameters.
#   2. Create commands.rs with pub fn handle_slash_command(...) with the same signature.
#   3. Move the entire function body. Some helper functions it calls (like
#      show_command_info_popup, detect_language_from_path) may also need to move.
#   4. The function already uses watcher:: and helpers:: imports from prior extraction.
#   5. Update lib.rs to declare `pub mod commands;`
#   6. Update main.rs to call commands::handle_slash_command().
# DONE WHEN: `cargo build` succeeds, `cargo test` passes, handle_slash_command is no
#   longer defined in main.rs (only called from it), and main.rs is ~3800 lines shorter.

(B) Extract RPC response handling from main.rs into poor-cli-tui/src/handler.rs +refactor
# PURPOSE: The ServerMsg match arms in the event loop handle streaming chunks,
#   tool execution events, permission prompts, plan review UI, and error display.
#   This is display/business logic that should be separate from event dispatch.
# WHAT TO DO:
#   1. Identify the match arms in the event loop that handle ServerMsg variants like
#      ChatResponse, StreamingChunk, ToolEvent, PermissionRequest, PlanReview, Error, etc.
#   2. Create handler.rs with functions like handle_chat_response(), handle_tool_event(),
#      handle_permission_request(), etc. — or a single dispatch function.
#   3. Each handler takes &mut App and the variant's data, updates app state / pushes messages.
#   4. Update lib.rs and the event loop to delegate to handler:: functions.
# DONE WHEN: `cargo build` succeeds, `cargo test` passes, ServerMsg match arms in
#   the event loop are thin delegations to handler:: functions.

(B) Extract multiplayer host/join logic from main.rs into poor-cli-tui/src/multiplayer.rs +refactor
# PURPOSE: main.rs contains logic for --remote-url/--remote-room/--remote-token CLI
#   args, WebSocket bridge setup, and multiplayer-specific ServerMsg handling.
# WHAT TO DO:
#   1. Search main.rs for "remote_url", "remote_room", "remote_token", "multiplayer",
#      "bridge" to find all multiplayer-related code blocks.
#   2. Create multiplayer.rs with connection setup, bridge spawning, and any
#      multiplayer-specific message handling.
#   3. Update lib.rs to declare `pub mod multiplayer;`
# NOTE: This depends on event.rs and handler.rs being extracted first (or at least
#   having clear boundaries), since multiplayer messages flow through ServerMsg.
# DONE WHEN: `cargo build` succeeds, `cargo test` passes, multiplayer logic is
#   cleanly separated from the main event loop.

# ============================================================================
# CHECKPOINT SYSTEM — Consolidate and harden
# ============================================================================

(B) Reduce checkpoint_strategies.py from 6 classes to 3 by merging related classes +checkpoint +refactor
# PURPOSE: checkpoint_strategies.py has 6 classes with overlapping responsibilities.
#   Merging reduces cognitive overhead and eliminates internal coupling.
# WHAT TO DO:
#   1. Read poor_cli/checkpoint_strategies.py — identify these classes:
#      - ProjectDetector: detects project type (Rust/Python/JS/etc)
#      - FileFilter: filters files based on ProjectIgnoreRules
#      - SmartCompressor: handles compression strategy selection + compress/decompress
#      - PartialCheckpointStrategy: incremental checkpoint logic
#      - CompressionStrategy (Enum): NONE/ZLIB_FAST/ZLIB_DEFAULT/ZLIB_MAX
#      - ProjectIgnoreRules (dataclass): patterns/directories/extensions
#   2. Merge ProjectDetector + FileFilter into a single class (e.g., ProjectFileFilter)
#      that both detects project type AND filters files. They share ignore rules already.
#   3. Merge SmartCompressor + PartialCheckpointStrategy into a single class (e.g.,
#      SmartCheckpointStrategy) that handles both compression and incremental detection.
#   4. Keep CompressionStrategy enum and ProjectIgnoreRules dataclass as-is (they're data types).
#   5. Update all imports across the codebase (grep for the old class names).
#   6. Note: get_ignore_rules() was recently made cacheable with _ignore_rules_cache.
# DONE WHEN: checkpoint_strategies.py has 3 main classes (down from 6), all imports
#   updated, and `ruff check` passes.

# ============================================================================
# MULTIPLAYER — Refine as core differentiator
# ============================================================================

(B) Audit poor_cli/multiplayer.py for race conditions in RoomState +multiplayer +bugfix
# PURPOSE: RoomState has an asyncio.Queue (request_queue) and asyncio.Lock
#   (dispatch_lock). While asyncio is single-threaded, the interaction between
#   queue put/get and lock acquire/release across multiple coroutines needs verification.
# WHAT TO DO:
#   1. Read poor_cli/multiplayer.py — focus on _handle_rpc_message() (~line 700) and
#      the queue worker loop (~line 810).
#   2. Trace the lifecycle: message arrives → queued → worker dequeues → acquires
#      dispatch_lock → dispatches → releases lock. Verify no deadlock scenarios.
#   3. Check: what happens if a connection disconnects while its request is queued?
#      Is the queued request cleaned up? Does the worker handle stale connection_ids?
#   4. Add an integration test in tests/ that spawns a MultiplayerHost, connects 2+
#      concurrent prompters to the same room, sends overlapping chat requests, and
#      verifies responses are correctly routed (no cross-contamination).
# DONE WHEN: audit documented (comments or test docstring), integration test passes,
#   any discovered race conditions are fixed.

(B) Add connection health checks (ping/pong heartbeat) to multiplayer.py +multiplayer +feature
# PURPOSE: If a client's network drops silently (no TCP FIN), the server holds stale
#   ConnectionState entries forever. WebSocket ping/pong detects these.
# WHAT TO DO:
#   1. In MultiplayerHost, add a background asyncio.Task that runs every 30s.
#   2. For each ConnectionState in each room, call ws.ping() and track last_pong time.
#   3. If no pong received within 60s, close the WebSocket and remove the connection.
#   4. Add a `last_pong: float` field to ConnectionState dataclass (line ~30).
#   5. Handle the pong response in the WebSocket message loop (aiohttp sends pong
#      automatically, but you need to track when it arrives — use ws.receive() which
#      returns WSMsgType.PONG).
# DONE WHEN: stale connections are detected and cleaned up within ~90s of network loss.

(B) Add a /kick command to multiplayer host +multiplayer +feature
# PURPOSE: Host should be able to remove a misbehaving or idle connection.
# WHAT TO DO:
#   1. Add a new RPC method "poor-cli/kickMember" to MultiplayerHost that accepts
#      {room: string, connection_id: string} params.
#   2. Only the host (or admin role) should be allowed to call this.
#   3. Implementation: find the ConnectionState, close its WebSocket with code 4001
#      and reason "Kicked by host", remove from room.members, broadcast a room event.
#   4. Add the method to _VIEWER_BLOCKED_METHODS so viewers can't kick.
#   5. In the Rust TUI (main.rs handle_slash_command), add "/kick <connection_id>"
#      that sends this RPC method.
# DONE WHEN: /kick <id> from host TUI disconnects the target and broadcasts an event.

(B) Add rate limiting per connection in multiplayer.py +multiplayer +feature
# PURPOSE: A single prompter could flood the request queue with rapid chat messages,
#   starving other prompters.
# WHAT TO DO:
#   1. Add a simple token bucket or sliding window rate limiter per ConnectionState.
#   2. Track timestamps of recent requests (e.g., deque with maxlen=10).
#   3. In _handle_rpc_message(), before queuing a request, check if the connection
#      has exceeded the rate limit (e.g., 10 requests per 60 seconds for chat methods).
#   4. If exceeded, return a JSON-RPC error with code -32029 and message "Rate limited".
#   5. Make the limit configurable (requests_per_minute field on MultiplayerHost).
# DONE WHEN: rapid-fire chat requests from a single connection are rejected after
#   hitting the limit, while other connections can still queue requests.

(B) Add a room participant list RPC method +multiplayer +feature
# PURPOSE: TUI clients need to display who's in the room, their roles, and status.
# WHAT TO DO:
#   1. Add RPC method "poor-cli/listRoomMembers" that returns an array of:
#      {connection_id, role, connected_at, last_active, is_active_prompter}
#   2. connected_at should already exist on ConnectionState (or add it as a float timestamp).
#   3. Any authenticated connection should be able to call this (not viewer-blocked).
#   4. In the Rust TUI, add "/who" or "/members" slash command that calls this method
#      and displays results in a popup or system message.
# DONE WHEN: /who in TUI shows the list of connected members with their roles.

(B) Add automatic reconnection logic in the TUI Rust client for multiplayer +multiplayer +feature
# PURPOSE: When the WebSocket drops (network blip, server restart), the TUI should
#   reconnect automatically instead of requiring manual restart.
# WHAT TO DO:
#   1. In poor-cli-tui/src/main.rs, find the multiplayer/bridge connection code
#      (search for "remote_url", "tungstenite", "WebSocket").
#   2. Wrap the WebSocket read loop in a reconnection loop with exponential backoff
#      (1s, 2s, 4s, 8s, max 30s).
#   3. On reconnect, re-authenticate with the same room/token.
#   4. Send a ServerMsg::SystemMessage to the UI indicating reconnection status.
#   5. Cap max reconnection attempts (e.g., 10) before giving up with an error.
# DONE WHEN: TUI auto-reconnects after a brief network interruption and resumes
#   the multiplayer session without user intervention.

(B) Add an event broadcast mechanism for viewer streaming +multiplayer +feature
# PURPOSE: Viewers currently only see final results. They should see real-time
#   streaming output as the active prompter's session generates it.
# WHAT TO DO:
#   1. In multiplayer.py, when a streaming response is generated during dispatch,
#      broadcast each chunk to all viewer connections in the room.
#   2. Add a "poor-cli/streamingChunk" notification type that the server sends to
#      viewer WebSockets (no id, just method + params with {content, requestId}).
#   3. In the Rust TUI bridge mode, handle this notification and display it in the
#      chat panel (similar to how local streaming chunks are rendered).
#   4. Only broadcast to viewers and the requesting prompter, not other prompters.
# DONE WHEN: A viewer connected to a room sees streaming text appear in real-time
#   as the active prompter's request is being processed.

# ============================================================================
# NEOVIM PLUGIN — First-class citizen fixes and features
# ============================================================================

(B) Add virtual text diagnostic integration for poor-cli suggestions +neovim +feature
# PURPOSE: Currently poor-cli suggestions only appear in the chat panel. Showing
#   them inline as Neovim diagnostics makes them more visible and actionable.
# WHAT TO DO:
#   1. Create a new file nvim-poor-cli/lua/poor-cli/diagnostics.lua
#   2. Define a diagnostic namespace: vim.diagnostic.set(ns, bufnr, diagnostics)
#   3. When poor-cli returns suggestions that reference specific files/lines (e.g.,
#      from /fix, /review, or error analysis), parse the file:line references.
#   4. Create vim.Diagnostic entries with severity INFO or HINT, message = suggestion text.
#   5. Add a config option `diagnostics_enabled = false` (opt-in) in config.lua.
#   6. Add a command :PoorCliDiagnostics to toggle the feature.
#   7. Clear diagnostics when the chat is cleared or a new request is sent.
# DONE WHEN: After running /review or similar, suggestions with file:line references
#   appear as inline diagnostics in the referenced buffers.

(B) Add a telescope.nvim picker for checkpoint browsing and restoration +neovim +feature
# PURPOSE: Users should be able to browse and restore checkpoints from within Neovim
#   using the familiar telescope.nvim interface.
# WHAT TO DO:
#   1. Create nvim-poor-cli/lua/poor-cli/telescope.lua (or telescope_picker.lua).
#   2. Implement a telescope picker that calls the "poor-cli/listCheckpoints" RPC method
#      to get checkpoint data (id, description, created_at, file_count, size).
#   3. Display checkpoints as telescope entries with preview showing affected files.
#   4. On selection (<CR>), call "poor-cli/restoreCheckpoint" RPC with the checkpoint_id.
#   5. Add a confirmation prompt before restore (telescope actions or vim.fn.confirm).
#   6. Register via :PoorCliCheckpoints command and optional keymap in config.lua.
#   7. telescope.nvim should be an optional dependency — gracefully handle its absence.
# DONE WHEN: :PoorCliCheckpoints opens a telescope picker, user can browse and restore
#   checkpoints. Works without telescope installed (shows error message).

# ============================================================================
# STABILITY — Error handling, security, hardening
# ============================================================================

(B) Audit all unwrap() calls in the Rust TUI codebase +security +refactor
# PURPOSE: unwrap() panics on None/Err, crashing the TUI. Critical paths like event
#   handling and RPC parsing should propagate errors gracefully instead.
# WHAT TO DO:
#   1. Run `grep -rn 'unwrap()' poor-cli-tui/src/` to find all ~47 call sites.
#   2. Categorize each as: (a) truly infallible (e.g., regex compile with known-good
#      pattern), (b) in a critical path (event loop, RPC parsing, file I/O), or
#      (c) in a test.
#   3. For category (b), replace with proper error handling:
#      - `.unwrap_or_default()` for display values
#      - `match` or `if let` for optional values
#      - `?` operator where the function returns Result
#      - `.unwrap_or_else(|e| { log error; default })` for recoverable errors
#   4. Focus on: rpc.rs (RPC message parsing), main.rs (event loop), app.rs (state updates).
#   5. Category (a) can keep unwrap() but add a comment explaining why it's safe.
# DONE WHEN: `cargo build` succeeds, no unwrap() calls remain in event handling or
#   RPC parsing paths, tests pass. Infallible unwraps are documented.

(B) Replace broad Exception catches in poor_cli/tools_async.py +bugfix +refactor
# PURPOSE: tools_async.py has many `except Exception:` blocks that catch everything
#   including KeyboardInterrupt and SystemExit. This masks bugs and makes debugging hard.
# WHAT TO DO:
#   1. Read poor_cli/tools_async.py — it's the async tool execution engine.
#   2. Identify the 10 most critical tool execution paths (file read/write, command
#      execution, directory listing, search, etc.).
#   3. For each, determine what exceptions are actually expected:
#      - File operations: FileNotFoundError, PermissionError, IsADirectoryError, OSError
#      - Command execution: subprocess.SubprocessError, FileNotFoundError, OSError
#      - JSON parsing: json.JSONDecodeError, KeyError, TypeError
#   4. Replace `except Exception as e:` with the specific types.
#   5. Keep a broad `except Exception:` only at the outermost dispatcher level as a
#      safety net (with logging).
#   6. Import FileOperationError, CommandExecutionError from poor_cli/exceptions.py.
# DONE WHEN: At least 10 critical tool paths use specific exception types.
#   `ruff check` passes.

(B) Consolidate except clauses in poor_cli/error_recovery.py +refactor
# PURPOSE: error_recovery.py has ~25 except clauses that each map an error type to
#   a user-friendly suggestion. There's significant duplication in the mapping logic.
# WHAT TO DO:
#   1. Read poor_cli/error_recovery.py — note the pattern: each handler catches a
#      specific error, extracts a message, and returns a RecoverySuggestion with
#      description, commands, and severity.
#   2. Create a dict/list mapping: {error_pattern: RecoverySuggestion} that captures
#      the common cases (API key errors, import errors, permission errors, etc.).
#   3. Create a helper function like `_match_error_to_suggestion(error: Exception)`
#      that iterates the mapping and returns the first matching suggestion.
#   4. Replace the 25 individual except blocks with a smaller set that delegates to
#      this mapping function.
#   5. Keep any handlers that need custom logic (e.g., those that inspect error attributes
#      beyond the message string) as separate handlers.
# DONE WHEN: error_recovery.py has significantly fewer except clauses (target: <10),
#   the mapping is centralized, and existing error recovery behavior is preserved.

# ============================================================================
# PROVIDER SYSTEM
# ============================================================================

(B) Add round-trip translation unit tests for tool_translator.py +provider +infra
# PURPOSE: tool_translator.py converts tool definitions between canonical (Gemini),
#   OpenAI, Anthropic, and Ollama formats. No tests verify round-trip fidelity.
# WHAT TO DO:
#   1. Create tests/test_tool_translator.py
#   2. Define a canonical tool definition with various parameter types (string, int,
#      array, object, enum, required/optional fields).
#   3. For each provider (Gemini, OpenAI, Anthropic, Ollama):
#      a. Convert canonical → provider format using ToolTranslator.to_provider_format()
#      b. Convert provider → canonical using ToolTranslator.from_provider_format()
#      c. Assert the round-tripped result matches the original (modulo known lossy fields).
#   4. Test edge cases: empty parameters, nested objects, tools with no description.
#   5. Import from poor_cli.providers.tool_translator — the class is ToolTranslator,
#      provider types are in ProviderType enum.
# DONE WHEN: `pytest tests/test_tool_translator.py` passes with round-trip tests
#   for all 4 providers.

(B) Abstract provider-specific error parsing into per-provider methods +provider +refactor
# PURPOSE: poor_cli/_server.py has inline error parsing that checks if an exception
#   message looks like a Gemini error, OpenAI error, or Anthropic error, each with
#   different JSON structures. This should be on the provider classes themselves.
# WHAT TO DO:
#   1. Read poor_cli/_server.py — search for error handling that parses provider-specific
#      error formats (look for "gemini", "openai", "anthropic" in error handling code,
#      or references to _sanitize_exception_message).
#   2. Read poor_cli/providers/ — each provider (gemini_provider.py, openai_provider.py,
#      anthropic_provider.py, ollama_provider.py) has a provider class.
#   3. Add an `error_from_response(cls, exception: Exception) -> Dict[str, Any]` classmethod
#      or instance method to each provider that extracts structured error info
#      (error_code, message, retryable, details) from provider-specific exception formats.
#   4. In _server.py, replace the inline parsing with calls to the appropriate provider's
#      error_from_response() method based on the active provider.
#   5. The error_formatter.py module (poor_cli/server/error_formatter.py) already has
#      _sanitize_exception_message — the provider methods should produce input for it.
# DONE WHEN: Provider-specific error format knowledge lives in provider classes,
#   not in _server.py. _server.py calls provider.error_from_response() instead of
#   doing format detection inline.
