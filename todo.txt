(B) implement SVG text wrapping for long entity names in src/render/svg_render.rs: split names exceeding a character threshold across multiple <tspan> elements with dy offsets +feature
(B) add SVG clipping rects in src/render/svg_render.rs: wrap each timeline region in a <clipPath> element to prevent entity bars and labels from rendering outside their timeline's bounding box +feature
(B) wire export width/height CLI args to SVG viewBox dimensions in src/render/svg_render.rs instead of hardcoded 1200px width, computing aspect ratio from layout total_width and total_lanes +feature
(B) apply type registry render hints to SVG output in src/render/svg_render.rs: read @color from entity's type definition for fill color, @shape for rect vs circle vs diamond, @icon for optional Unicode icon prefix on label +feature
(B) replace bitmap PDF export with vector PDF in src/render/pdf_render.rs: use printpdf line/rect/text primitives to draw timeline regions, entity bars, relationship edges, and labels directly as vector objects +feature
(B) make PNG DPI configurable: add --dpi <N> flag to export subcommand in src/cli/commands.rs, pass through to png_render.rs instead of hardcoded 150 +feature
(B) wire config file loading in main.rs: after CLI parsing, call config::loader::load_config() and merge loaded config values (theme, default_format, default_width) with CLI args, CLI taking precedence +infra
(B) wire --theme CLI flag to Theme selection in main.rs: match flag value against "dark"/"light"/custom TOML path and pass resolved Theme to layout, TUI, and render modules instead of always using Theme::default() +infra
(B) wire --verbose CLI flag to structured logging: add env_logger dependency, initialize with RUST_LOG or --verbose flag, replace print!() debug statements in evaluator/parser with log::debug!() macros +infra
(B) support hex color shorthand #RGB and alpha #RRGGBBAA in src/config/theme.rs hex_to_color(): detect 4-char and 9-char hex strings and expand/parse accordingly +feature
(B) replace unwrap() calls in src/lang/parser.rs with proper Result/Option propagation, returning ParseError with span context instead of panicking +stability
(B) replace unwrap() calls in src/eval/evaluator.rs with RuntimeError returns, especially in timeline/entity/relationship construction where field access can fail +stability
(B) replace unwrap() calls in src/render/svg_render.rs with Result propagation, surfacing render errors to the caller instead of panicking on malformed layout data +stability
(B) replace unwrap() calls in src/layout/engine.rs with Result propagation, returning LayoutError for edge cases like missing timeline references or empty entity lists +stability
(B) add error handling for file read failures after watcher change event in src/web/watcher.rs: catch io::Error and send error message over WebSocket channel instead of silently dropping the event +stability
(B) add ordering support to query DSL in src/eval/query.rs: parse "order by <field> asc|desc" clause, sort query results by the specified entity attribute before returning +feature
(B) add temporal range query support to query DSL in src/eval/query.rs: parse "overlaps <date>..<date>" clause to filter entities whose appears_on range overlaps the given range +feature
(B) improve query parser whitespace/quoting robustness in src/eval/query.rs: trim extra whitespace between tokens, handle both single and double quoted string values, handle missing quotes gracefully +feature
(B) populate web viewer filter sidebar dynamically in src/web/server.rs: on page load and WebSocket update, send JSON with entity type list and timeline name list, populate the filter checkboxes in the HTML via JavaScript +feature
(B) implement smooth WebSocket live-reload in src/web/server.rs: on file change, send updated SVG content via WebSocket message and replace the SVG DOM element in-place via JavaScript instead of reloading the entire page +feature
(B) add :entities meta-command to REPL in main.rs run_repl(): print a table of all entities with columns (name, type, timeline, time_range) when user types ":entities" +feature
(B) add :rels meta-command to REPL in main.rs run_repl(): print a table of all relationships with columns (source, label, target, directed, temporal_scope) when user types ":rels" +feature
(B) add :validate meta-command to REPL in main.rs run_repl(): run validator::validate(&world) and print the ValidationReport when user types ":validate" +feature
(B) improve REPL parse error recovery in main.rs run_repl(): when a multi-line block fails to parse, display the error with line numbers relative to the input block and prompt for re-entry instead of silently discarding +feature
(B) add crates.io metadata to Cargo.toml: description, license (MIT or Apache-2.0), repository URL, keywords ["dsl", "timeline", "visualization", "narrative"], categories ["command-line-utilities", "visualization"], readme = "README.md" +infra
(B) add LICENSE file to repo root with the chosen open-source license text (MIT recommended for max adoption) +infra
(B) create Homebrew formula: write a Formula class in a separate tap repo or inline formula that downloads the release binary for macOS arm64/x86_64, installs to bin/seuss +infra
(B) add GitHub Actions CI workflow in .github/workflows/ci.yml: on push/PR run cargo fmt --check, cargo clippy -- -D warnings, cargo test, cargo build --release for ubuntu-latest and macos-latest +infra
(B) add GitHub Actions release workflow in .github/workflows/release.yml: on tag push, build release binaries for macOS arm64, macOS x86_64, Linux x86_64, create GitHub Release with attached binaries +infra
(B) add CSS animation fallback alongside SMIL in src/render/animation.rs: generate @keyframes rules and animation CSS properties as a <style> block for browsers that don't support SMIL (Chrome deprecated SMIL) +feature
(B) enforce field type validation during entity creation in src/eval/evaluator.rs: when entity has a type_ref, look up TypeDef in registry, verify each provided field matches the declared FieldType, emit RuntimeError for mismatches +feature
(B) add multi-level inheritance cycle detection in src/ext/type_registry.rs: when registering a type with a parent, walk the parent chain and return error if the type appears in its own ancestry +stability
(B) validate type_ref exists in type registry during entity evaluation in src/eval/evaluator.rs: when processing EntityDecl with a type_ref, verify the type name exists in World.type_registry, emit RuntimeError with suggestion for typos if not found +feature
(B) add relationship diff to run_diff in main.rs: extend the diff output to compare relationship sets between two files, showing added/removed/changed relationships with labels and directionality +feature
(B) add colorized diff output in main.rs run_diff(): use crossterm::style to color added items green, removed items red, changed items yellow when outputting diff results to a terminal +feature
(B) add --format flag validation in src/cli/commands.rs or main.rs: reject unsupported export format strings with a descriptive error listing supported formats (svg, png, pdf) instead of falling through silently +stability
(B) add entity attribute inheritance resolution: when accessing a field on an entity whose type has a parent type, walk up the type chain to find inherited default values if the field is not set directly on the entity, in src/eval/evaluator.rs +feature
